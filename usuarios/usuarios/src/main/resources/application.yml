<<<<<<< HEAD
server:
  port: 8080

# Configuracion de mysql
spring:
  application:
    name: users
  datasource:
    url: jdbc:mysql://mysql-users:3306/users_db
    username: aurastore
    password: aurastore123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQLDialect
  # Flyway: habilitar migraciones
  flyway:
    enabled: true
    locations: classpath:db/migration

# Configuracion de Eureka
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    lease-renewal-interval-in-seconds: 30
    lease-expiration-duration-in-seconds: 90

# Actuator
=======
# server:
#   port: 8080

# # Configuracion de mysql
# spring:
#   application:
#     name: users
#   datasource:
#     url: jdbc:mysql://mysql-users:3306/users_db
#     username: aurastore
#     password: aurastore123
#     driver-class-name: com.mysql.cj.jdbc.Driver
#   jpa:
#     hibernate:
#       ddl-auto: none
#     database-platform: org.hibernate.dialect.MySQLDialect
#   # Flyway: habilitar migraciones
#   flyway:
#     enabled: true
#     locations: classpath:db/migration

# # Configuracion de Eureka
# eureka:
#   client:
#     service-url:
#       defaultZone: http://eureka-server:8761/eureka/
#     register-with-eureka: true
#     fetch-registry: true
#   instance:
#     lease-renewal-interval-in-seconds: 30
#     lease-expiration-duration-in-seconds: 90

# # Actuator
# management:
#   endpoints:
#     web:
#       exposure:
#         include: health
#   endpoint:
#     health:
#       show-details: always

# # Logs
# logging:
#   file:
#     name: /app/logs/${spring.application.name}/${spring.application.name}.log

#   logback:
#     rollingpolicy:
#       max-file-size: 10mb
#       max-history: 10

# # Swagger
# springdoc:
#   api-docs:
#     path: /v3/api-docs
#   swagger-ui:
#     path: /index.html
#     disable-swagger-default-url: true

server:
  port: 8080

spring:
  application:
    name: users

  datasource:
    url: jdbc:mysql://localhost:3310/users_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true

  flyway:
    enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

>>>>>>> eliascarcamo
management:
  endpoints:
    web:
      exposure:
<<<<<<< HEAD
        include: health
=======
        include: health,info
>>>>>>> eliascarcamo
  endpoint:
    health:
      show-details: always

<<<<<<< HEAD
# Logs
logging:
  file:
    name: /app/logs/${spring.application.name}/${spring.application.name}.log

  logback:
    rollingpolicy:
      max-file-size: 10mb
      max-history: 10

# Swagger
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /index.html
    disable-swagger-default-url: true
=======
logging:
  file:
    name: logs/users.log

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
>>>>>>> eliascarcamo
